# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/nix-fast-build-trial.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  laptop:
    environment: ci
    name: laptop (x86_64-linux)
    runs-on: ubuntu-24.04
    steps:
    - uses: wimpysworld/nothing-but-nix@687c797a730352432950c707ab493fcc951818d7
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with:
        persist-credentials: false
    - uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24
    - uses: Mic92/hestia@f1f4df2801140a36398ed423533c8460618539df
      with:
        upstream-cache-filter: true
        upstream-cache-key-names: cache.nixos.org-1 nix-community.cachix.org-1 noctalia.cachix.org-1
          niri.cachix.org-1 bbigras-nix-config.cachix.org-1
    - uses: cachix/cachix-action@5f2d7c5294214f71b873db4b969586b980625e71
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        extraPullNames: nix-community,noctalia,niri
        name: bbigras-nix-config
    - name: nix-fast-build (skip cached)
      run: |-
        started_at="$(date +%s)"
        trap 'printf "nix-fast-build elapsed_seconds=%s\\n" "$(( $(date +%s) - started_at ))"' EXIT
        nix run 'git+file:.#nix-fast-build' -- --no-nom --skip-cached --retries=3 --option accept-flake-config true --flake='git+file:.#nixosConfigurations.laptop.config.system.build.toplevel'
    timeout-minutes: 120
name: nix-fast-build-trial
'on':
  workflow_dispatch: {}
permissions: {}
